import './styles.css';
import { dailyBrief, eventDateKey, formatDay, formatTime, localDateKey, parseIcs, validateImport, type AppData, type CalendarEvent, type SideNote } from './domain';
import { discardDemoData, loadData, saveData, type StorageMode } from './db';

declare const __BUILD_ID__: string;

const app = document.querySelector<HTMLDivElement>('#app')!;
const quickPhrases = [
  { label: 'Start callback note', text: 'Call back' },
  { label: 'Start access note', text: 'Confirm access details' },
  { label: 'Start supplier note', text: 'Check supplier timing' },
  { label: 'Start follow-up note', text: 'Send follow-up' },
];
const esc = (value = '') => value.replace(/[&<>'"]/g, (char) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;' })[char]!);
const uid = () => crypto.randomUUID?.() ?? `${Date.now()}-${Math.random().toString(16).slice(2)}`;
let data: AppData = { version: 1, events: [], notes: [] };
let selectedDate = localDateKey();
let storageMode: StorageMode = 'real';
let editingId: string | null = null;
let pendingMessage = '';
let updatingApp = false;

function currentRoute(): 'home' | 'demo' | 'not-found' {
  const path = location.pathname.replace(/\/+$/, '') || '/';
  if (path === '/' && new URL(location.href).searchParams.get('demo') === '1') return 'demo';
  if (path === '/') return 'home';
  if (path === '/demo') return 'demo';
  return 'not-found';
}

function sampleData(): AppData {
  return { version: 1, events: [
    { id: 'demo-harbour-heat', summary: 'Harbour House boiler visit', start: '2026-08-28T08:30:00.000Z', end: '2026-08-28T09:30:00.000Z', location: '14 Market Lane' },
    { id: 'demo-riverside', summary: 'Riverside Salon access check', start: '2026-08-28T11:00:00.000Z', end: '2026-08-28T11:45:00.000Z', location: '2 Wharf Street' },
    { id: 'demo-pine', summary: 'Pine Studio delivery', start: '2026-08-28T14:15:00.000Z', end: '2026-08-28T15:00:00.000Z', location: '8 Station Yard' },
  ], notes: [
    { id: 'demo-note-gate', date: '2026-08-28', eventId: 'demo-harbour-heat', anchorTime: '08:15', text: 'Call before arrival; the side gate is open after 8:00.', completed: false, reminder: 'due', updatedAt: '2026-08-27T16:30:00.000Z' },
    { id: 'demo-note-key', date: '2026-08-28', eventId: 'demo-riverside', anchorTime: '10:50', text: 'Ask for the key safe code at reception.', completed: false, reminder: 'none', updatedAt: '2026-08-27T16:32:00.000Z' },
    { id: 'demo-note-supplier', date: '2026-08-28', eventId: 'demo-pine', anchorTime: '13:45', text: 'Supplier says replacement filter arrives by lunch.', completed: true, reminder: 'acknowledged', updatedAt: '2026-08-28T07:00:00.000Z' },
  ] };
}

function setMetadata(route: ReturnType<typeof currentRoute>): void {
  const meta = route === 'demo' ? ['Demo — Booking Side Notes', 'Try sample appointments and side notes without saving anything to your data.', '/demo'] : route === 'not-found' ? ['Page not found — Booking Side Notes', 'This Booking Side Notes page could not be found.', '/404'] : ['Booking Side Notes — side notes beside appointments', 'Keep callbacks, access details, and follow-ups beside appointments without changing your calendar.', '/'];
  document.title = meta[0];
  document.querySelector('meta[name="description"]')?.setAttribute('content', meta[1]);
  document.querySelector('link[rel="canonical"]')?.setAttribute('href', `https://booking-side-notes.sociobot.in${meta[2]}`);
  document.querySelectorAll<HTMLMetaElement>('meta[property="og:title"],meta[name="twitter:title"]').forEach((item) => item.content = meta[0]);
  document.querySelectorAll<HTMLMetaElement>('meta[property="og:description"],meta[name="twitter:description"]').forEach((item) => item.content = meta[1]);
}

function eventsForDay(): CalendarEvent[] { return data.events.filter((event) => eventDateKey(event) === selectedDate).sort((a, b) => a.start.localeCompare(b.start)); }
function notesForDay(): SideNote[] { return data.notes.filter((note) => note.date === selectedDate).sort((a, b) => Number(a.completed) - Number(b.completed) || (a.anchorTime || '99:99').localeCompare(b.anchorTime || '99:99')); }
function appointmentMarkup(event: CalendarEvent): string {
  const noteCount = notesForDay().filter((note) => note.eventId === event.id).length;
  return `<li class="appointment"><div class="time-rail" aria-hidden="true"></div><div class="appointment-time"><time datetime="${esc(event.start)}">${esc(formatTime(event.start))}</time>${event.end ? `<span>to ${esc(formatTime(event.end))}</span>` : ''}</div><div class="appointment-copy"><h3>${esc(event.summary)}</h3>${event.location ? `<p><span aria-hidden="true">⌖</span> ${esc(event.location)}</p>` : ''}<p class="note-count">${noteCount} side ${noteCount === 1 ? 'note' : 'notes'} · bookable time unchanged</p></div><button class="button ghost compact add-for-event" data-event-id="${esc(event.id)}">Add side note<span class="sr-only"> for ${esc(event.summary)}</span></button></li>`;
}

function noteMarkup(note: SideNote, index: number): string {
  const linked = data.events.find((event) => event.id === note.eventId)?.summary;
  const association = linked ? esc(linked) : note.eventId ? 'Unlinked side note' : 'For the whole day';
  const reminder = note.reminder === 'due' ? 'Reminder due' : note.reminder === 'acknowledged' ? 'Reminder acknowledged' : '';
  const reminderAction = note.reminder === 'due' ? 'Mark reminder acknowledged' : 'Mark reminder due';
  return `<li class="note-card ${note.completed ? 'is-complete' : ''}"><label class="check-wrap"><input class="note-check" data-note-id="${note.id}" type="checkbox" ${note.completed ? 'checked' : ''}><span class="custom-check" aria-hidden="true"></span><span class="sr-only">Mark “${esc(note.text)}” ${note.completed ? 'not done' : 'done'}</span></label><span class="map-pin" aria-hidden="true"><span>${index + 1}</span></span><div class="note-body"><div class="note-meta"><span>${esc(note.anchorTime || 'Any time')}</span><span>${association}</span>${reminder ? `<span class="reminder-state status-${note.reminder}">${reminder}</span><button class="reminder-status" data-reminder-id="${note.id}">${reminderAction}</button>` : ''}</div><p>${esc(note.text)}</p><span class="completion-label">${note.completed ? 'Done' : 'Open'}</span></div><div class="note-actions"><button class="icon-button edit-note" data-note-id="${note.id}" aria-label="Edit ${esc(note.text)}">✎</button><button class="icon-button delete-note" data-note-id="${note.id}" aria-label="Delete ${esc(note.text)}">×</button></div></li>`;
}

function header(): string { return `<header class="site-header"><a class="brand" href="/" data-route="/"><img src="/assets/app-mark.svg" width="40" height="40" alt=""><span>Booking Side Notes</span></a><nav class="site-nav" aria-label="Main navigation"><a href="/?demo=1" data-route="/?demo=1">Try demo</a><a href="/privacy/">Privacy</a><a href="/terms/">Terms</a></nav><span id="network-state" class="network-state"><span aria-hidden="true">●</span> ${navigator.onLine ? 'On device' : 'Offline · on device'}</span></header>`; }
function footer(): string { return `<footer><span>Side notes beside appointments, on this device.</span><nav aria-label="Footer"><a href="/demo" data-route="/demo">Demo</a><a href="/privacy/">Privacy</a><a href="/terms/">Terms</a></nav><span>Built by Param Factory · build ${__BUILD_ID__}</span></footer>`; }
function focusTitle(): void { requestAnimationFrame(() => { const heading = document.querySelector<HTMLElement>('#page-title'); heading?.focus(); const announcer = document.querySelector('#route-announcer'); if (announcer) announcer.textContent = heading?.textContent || ''; }); }
function bindRoutes(): void { document.querySelectorAll<HTMLAnchorElement>('[data-route]').forEach((link) => link.addEventListener('click', (event) => { event.preventDefault(); void navigate(link.dataset.route || '/'); })); }

function renderNotFound(): void {
  app.innerHTML = `${header()}<main id="main" class="fatal"><p class="eyebrow">Map reference missing</p><h1 id="page-title" tabindex="-1">This page was not found</h1><p>Check the address, or return to your appointments.</p><a class="button primary" href="/" data-route="/">Go to Booking Side Notes</a></main>${footer()}<div id="route-announcer" class="sr-only" aria-live="polite"></div>`;
  bindRoutes(); focusTitle();
}

function render(): void {
  const dayEvents = eventsForDay(); const dayNotes = notesForDay(); const openCount = dayNotes.filter((note) => !note.completed).length; const isDemo = storageMode === 'demo';
  const hasDemoGlance = isDemo && Boolean(dayEvents[0] && dayNotes[0]);
  const listedEvents = hasDemoGlance ? dayEvents.slice(1) : dayEvents;
  const listedNotes = hasDemoGlance ? dayNotes.slice(1) : dayNotes;
  const demoFirstLook = isDemo ? `<section class="demo-first-look" aria-labelledby="page-title"><div class="demo-intro"><p class="eyebrow">Sample workspace</p><h1 id="page-title" tabindex="-1">Try a sample appointment day</h1><p>Review one appointment and its side note, then edit anything below.</p></div>${dayEvents[0] && dayNotes[0] ? `<div class="demo-glance"><section aria-labelledby="demo-appointment-title"><p class="map-label">Appointment</p><h2 id="demo-appointment-title" class="sr-only">First sample appointment</h2><ol class="appointment-list">${appointmentMarkup(dayEvents[0])}</ol></section><section class="demo-note" aria-labelledby="demo-note-title"><p class="map-label">Side note beside it</p><h2 id="demo-note-title" class="sr-only">First sample side note</h2><ol class="note-list">${noteMarkup(dayNotes[0], 0)}</ol></section></div>` : ''}</section>` : '';
  app.innerHTML = `${header()}<main id="main">${isDemo ? `<aside class="demo-banner" aria-label="Demo mode"><strong>Demo — sample data, nothing is saved</strong><span>Try edits freely in this separate sample workspace.</span><button class="button ghost compact" id="reset-demo">Reset demo</button><button class="button secondary compact" id="start-real">Start for real</button></aside>` : ''}
${isDemo ? demoFirstLook : `<section class="hero" aria-labelledby="page-title"><div class="hero-copy"><h1 id="page-title" tabindex="-1">Keep side notes beside appointments</h1><p class="lede">For small appointment businesses that need callbacks and access details without changing bookable time.</p><div class="hero-actions"><a class="button primary" href="/?demo=1" data-route="/?demo=1">Try it with sample data</a><span>Opens a sample day with appointments and side notes.</span><label class="button secondary file-button" for="ics-file">Import an .ics file</label></div><ul class="hero-facts"><li>Stored on this device</li><li>Works offline after the first visit</li><li>No purchase required</li></ul></div><figure class="hero-art"><picture><source type="image/avif" srcset="/assets/hero-map-480.avif 480w, /assets/hero-map.avif 960w" sizes="(max-width: 680px) calc(100vw - 40px), 540px"><source type="image/webp" srcset="/assets/hero-map-480.webp 480w, /assets/hero-map.webp 960w" sizes="(max-width: 680px) calc(100vw - 40px), 540px"><img src="/assets/hero-map.jpg" width="960" height="640" alt="Topographic paper map showing appointments as teal routes and side notes as orange markers" fetchpriority="high" decoding="async"></picture><figcaption>Appointments stay on your calendar. Side notes stay here.</figcaption></figure></section>`}
<section class="day-tools" aria-label="Day controls"><div class="date-field"><label for="day-picker">Working day</label><input id="day-picker" type="date" value="${selectedDate}"></div><div class="tool-actions"><label class="button secondary file-button" for="ics-file">Import calendar <span class="file-kind">.ics</span></label><input class="visually-hidden-file" id="ics-file" type="file" accept=".ics,text/calendar"><button class="button primary" id="add-note">Add side note</button></div></section>
<div class="capacity-banner" role="note"><span class="capacity-zero">0</span><span><strong>minutes blocked</strong><br>by ${dayNotes.length} side ${dayNotes.length === 1 ? 'note' : 'notes'}</span><span class="capacity-rule" aria-hidden="true"></span><span>${openCount} open side ${openCount === 1 ? 'note' : 'notes'}</span></div>
<section class="workspace" aria-label="Daily workspace"><section class="route-panel" aria-labelledby="appointments-title"><div class="section-heading"><div><p class="map-label">Appointments / ${selectedDate.replaceAll('-', '·')}</p><h2 id="appointments-title">${esc(formatDay(selectedDate))}</h2></div><span class="count-stamp">${dayEvents.length} ${dayEvents.length === 1 ? 'appointment' : 'appointments'}</span></div>${dayEvents.length ? `<ol class="appointment-list">${listedEvents.map(appointmentMarkup).join('')}</ol>` : `<div class="empty-state"><div class="empty-contours" aria-hidden="true"><span></span><span></span><span></span></div><p class="empty-kicker">No appointments yet</p><h3>Import an appointment day</h3><p>Choose an .ics calendar export. We keep its ID, name, start and end times, and location on this device.</p><label class="button secondary" for="ics-file">Choose an .ics file</label></div>`}</section><section class="notes-panel" aria-labelledby="notes-title"><div class="section-heading"><div><p class="map-label">Side notes for this day</p><h2 id="notes-title">Side notes</h2></div><span class="count-stamp warm">${openCount} open</span></div><div class="quick-strip"><span>Quick note:</span>${quickPhrases.map((phrase) => `<button data-quick="${esc(phrase.text)}">${esc(phrase.label)}</button>`).join('')}</div>${dayNotes.length ? `<ol class="note-list">${listedNotes.map((note, index) => noteMarkup(note, index + (hasDemoGlance ? 1 : 0))).join('')}</ol>` : `<div class="empty-state notes-empty"><span class="large-pin" aria-hidden="true">+</span><h3>No side notes yet</h3><p>Add a callback, access detail, follow-up, or delay. A side note stays beside the day without changing bookable time.</p><button class="button primary" id="empty-add-note">Add a side note</button></div>`}</section></section>
<section class="how-it-works" aria-labelledby="how-title"><p class="map-label">How it works</p><h2 id="how-title">Keep the day clear in three steps</h2><ol><li><strong>Import appointments.</strong> Choose an .ics calendar export.</li><li><strong>Add side notes.</strong> Link a callback or access detail to an appointment.</li><li><strong>Print or export.</strong> Take a plain daily brief or backup file with you.</li></ol></section><section class="boundaries" aria-labelledby="boundaries-title"><p class="map-label">Your boundaries</p><h2 id="boundaries-title">What this tool does not do</h2><ul><li>It does not change your calendar.</li><li>It does not send notifications.</li><li>It stores appointment details and side notes in this browser.</li></ul></section>
<section class="brief-actions" aria-labelledby="brief-title"><div><p class="map-label">End-of-day sheet</p><h2 id="brief-title">Print or export the daily brief</h2><p>Print the daily brief or download a plain-text copy.</p></div><div class="tool-actions"><button class="button secondary" id="print-brief">Print daily brief</button><button class="button secondary" id="export-brief">Export daily brief</button></div></section><section class="data-section" id="data" aria-labelledby="data-title"><div class="data-column"><p class="map-label">Local backup</p><h2 id="data-title">Back up or restore your data</h2><p>Export appointments and side notes as one backup file. Importing a backup asks before it replaces data on this device.</p><div class="tool-actions"><button class="button ghost" id="export-data">Export backup</button><label class="button ghost file-button" for="json-file">Import backup</label><input class="visually-hidden-file" id="json-file" type="file" accept="application/json,.json"></div></div></section><pre class="print-brief" aria-hidden="true">${esc(dailyBrief(selectedDate, data))}</pre></main>${footer()}<dialog id="note-dialog" aria-labelledby="dialog-title"><form method="dialog" id="note-form"><div class="dialog-heading"><div><p class="map-label">Side note</p><h2 id="dialog-title">Add a side note</h2></div><button class="icon-button" value="cancel" formnovalidate aria-label="Close side note editor">×</button></div><p class="dialog-assurance">This creates no calendar event and does not change bookable time.</p><label for="note-text">Side note <span aria-hidden="true">*</span></label><textarea id="note-text" maxlength="500" rows="4" required aria-describedby="note-help"></textarea><p id="note-help" class="field-help">For example: “Call before arrival; side gate code is in the job sheet.”</p><div class="dialog-phrases"><span>Start with</span>${quickPhrases.map((phrase) => `<button type="button" data-dialog-quick="${esc(phrase.text)}">${esc(phrase.label)}</button>`).join('')}</div><div class="form-grid"><div><label for="note-time">Anchor time</label><input id="note-time" type="time"></div><div><label for="note-event">Beside appointment</label><select id="note-event"><option value="">For the whole day</option>${dayEvents.map((event) => `<option value="${esc(event.id)}">${esc(formatTime(event.start))} · ${esc(event.summary)}</option>`).join('')}</select></div></div><label class="reminder-choice" for="note-reminder"><input id="note-reminder" type="checkbox"><span><strong>Mark reminder due</strong><small>This only shows a due status. This app sends no notifications.</small></span></label><div class="dialog-actions"><button class="button ghost" value="cancel" formnovalidate>Cancel</button><button class="button primary" id="save-note" value="default">Save side note</button></div></form></dialog><div class="toast" id="toast" role="status" aria-live="polite" hidden></div><div class="update-toast" id="update-toast" role="status" hidden><span>An update is ready.</span><button class="button secondary compact" id="apply-update">Update app</button></div><div id="route-announcer" class="sr-only" aria-live="polite"></div>`;
  bindEvents(); if (pendingMessage) { const message = pendingMessage; pendingMessage = ''; queueMicrotask(() => showToast(message)); }
}

function showToast(message: string): void { const toast = document.querySelector<HTMLDivElement>('#toast'); if (toast) { toast.textContent = message; toast.hidden = false; window.setTimeout(() => { toast.hidden = true; }, 3500); } }
async function persist(message?: string): Promise<void> { try { await saveData(data, storageMode); if (message) pendingMessage = message; } catch (error) { pendingMessage = error instanceof Error ? error.message : 'Changes could not be saved.'; } }
function inputTime(iso?: string): string { if (!iso) return ''; const date = new Date(iso); return `${String(date.getHours()).padStart(2, '0')}:${String(date.getMinutes()).padStart(2, '0')}`; }
function openEditor(eventId?: string, note?: SideNote): void { editingId = note?.id ?? null; const dialog = document.querySelector<HTMLDialogElement>('#note-dialog')!; dialog.querySelector<HTMLHeadingElement>('#dialog-title')!.textContent = note ? 'Edit side note' : 'Add a side note'; dialog.querySelector<HTMLTextAreaElement>('#note-text')!.value = note?.text ?? ''; dialog.querySelector<HTMLInputElement>('#note-time')!.value = note?.anchorTime ?? (eventId ? inputTime(data.events.find((event) => event.id === eventId)?.start) : ''); dialog.querySelector<HTMLSelectElement>('#note-event')!.value = note?.eventId ?? eventId ?? ''; dialog.querySelector<HTMLInputElement>('#note-reminder')!.checked = note?.reminder === 'due'; dialog.showModal(); window.setTimeout(() => dialog.querySelector<HTMLTextAreaElement>('#note-text')!.focus(), 0); }
async function saveNote(event: SubmitEvent): Promise<void> { event.preventDefault(); if (event.submitter instanceof HTMLButtonElement && event.submitter.value === 'cancel') { document.querySelector<HTMLDialogElement>('#note-dialog')!.close(); return; } const text = document.querySelector<HTMLTextAreaElement>('#note-text')!.value.trim(); if (!text) return; const old = data.notes.find((note) => note.id === editingId); const note: SideNote = { id: old?.id ?? uid(), date: selectedDate, eventId: document.querySelector<HTMLSelectElement>('#note-event')!.value || undefined, anchorTime: document.querySelector<HTMLInputElement>('#note-time')!.value || undefined, text, completed: old?.completed ?? false, reminder: document.querySelector<HTMLInputElement>('#note-reminder')!.checked ? 'due' : 'none', updatedAt: new Date().toISOString() }; data.notes = old ? data.notes.map((item) => item.id === old.id ? note : item) : [...data.notes, note]; document.querySelector<HTMLDialogElement>('#note-dialog')!.close(); await persist(old ? 'Side note updated.' : 'Side note saved. Bookable time did not change.'); render(); }
async function importIcs(file: File): Promise<void> { try { const imported = parseIcs(await file.text()); const days = new Set(imported.map(eventDateKey)); const incomingIds = new Set(imported.map((event) => event.id)); const removed = data.events.filter((event) => days.has(eventDateKey(event)) && !incomingIds.has(event.id)); if (removed.length && !confirm(`Replace this day’s appointments? ${removed.length} missing appointment${removed.length === 1 ? '' : 's'} will be removed. Linked side notes will remain as unlinked side notes.`)) return; data.events = [...data.events.filter((event) => !days.has(eventDateKey(event))), ...imported]; selectedDate = eventDateKey(imported[0]); await persist(`${imported.length} ${imported.length === 1 ? 'appointment' : 'appointments'} imported. Your calendar was not changed.`); render(); } catch (error) { showToast(error instanceof Error ? error.message : 'The calendar could not be imported.'); } }
function download(filename: string, body: string, type: string): void { const url = URL.createObjectURL(new Blob([body], { type })); const anchor = document.createElement('a'); anchor.href = url; anchor.download = filename; anchor.click(); window.setTimeout(() => URL.revokeObjectURL(url), 0); }
async function navigate(path: string): Promise<void> { const target = new URL(path, location.origin); history.pushState({}, '', `${target.pathname}${target.search}`); await start(true); }

function bindEvents(): void {
  bindRoutes();
  document.querySelector<HTMLInputElement>('#day-picker')?.addEventListener('change', (event) => { selectedDate = (event.currentTarget as HTMLInputElement).value || localDateKey(); render(); });
  document.querySelector<HTMLInputElement>('#ics-file')?.addEventListener('change', (event) => { const file = (event.currentTarget as HTMLInputElement).files?.[0]; if (file) void importIcs(file); });
  document.querySelector('#add-note')?.addEventListener('click', () => openEditor()); document.querySelector('#empty-add-note')?.addEventListener('click', () => openEditor()); document.querySelectorAll<HTMLButtonElement>('.add-for-event').forEach((button) => button.addEventListener('click', () => openEditor(button.dataset.eventId)));
  document.querySelector<HTMLFormElement>('#note-form')?.addEventListener('submit', (event) => void saveNote(event));
  document.querySelectorAll<HTMLButtonElement>('[data-dialog-quick]').forEach((button) => button.addEventListener('click', () => { const field = document.querySelector<HTMLTextAreaElement>('#note-text')!; field.value = `${button.dataset.dialogQuick}: ${field.value}`; field.focus(); })); document.querySelectorAll<HTMLButtonElement>('[data-quick]').forEach((button) => button.addEventListener('click', () => { openEditor(); const field = document.querySelector<HTMLTextAreaElement>('#note-text')!; field.value = `${button.dataset.quick}: `; field.focus(); }));
  document.querySelectorAll<HTMLInputElement>('.note-check').forEach((input) => input.addEventListener('change', async () => { const note = data.notes.find((item) => item.id === input.dataset.noteId); if (!note) return; note.completed = input.checked; note.updatedAt = new Date().toISOString(); await persist('Side note status updated.'); render(); }));
  document.querySelectorAll<HTMLButtonElement>('.edit-note').forEach((button) => button.addEventListener('click', () => { const note = data.notes.find((item) => item.id === button.dataset.noteId); if (note) openEditor(undefined, note); })); document.querySelectorAll<HTMLButtonElement>('.delete-note').forEach((button) => button.addEventListener('click', async () => { const note = data.notes.find((item) => item.id === button.dataset.noteId); if (!note || !confirm(`Delete this side note?\n\n${note.text}`)) return; data.notes = data.notes.filter((item) => item.id !== note.id); await persist('Side note deleted.'); render(); }));
  document.querySelectorAll<HTMLButtonElement>('[data-reminder-id]').forEach((button) => button.addEventListener('click', async () => { const note = data.notes.find((item) => item.id === button.dataset.reminderId); if (!note) return; note.reminder = note.reminder === 'due' ? 'acknowledged' : 'due'; note.updatedAt = new Date().toISOString(); await persist('Reminder status updated.'); render(); }));
  document.querySelector('#print-brief')?.addEventListener('click', () => window.print()); document.querySelector('#export-brief')?.addEventListener('click', () => download(`side-notes-${selectedDate}.txt`, dailyBrief(selectedDate, data), 'text/plain')); document.querySelector('#export-data')?.addEventListener('click', () => download(`booking-side-notes-backup-${selectedDate}.json`, JSON.stringify(data, null, 2), 'application/json'));
  document.querySelector<HTMLInputElement>('#json-file')?.addEventListener('change', async (event) => { const file = (event.currentTarget as HTMLInputElement).files?.[0]; if (!file) return; let parsed: unknown; try { parsed = JSON.parse(await file.text()); } catch { showToast('This backup is not valid JSON. Choose a Booking Side Notes backup exported by this app.'); return; } try { const next = validateImport(parsed); if (!confirm(`Replace this device’s backup with ${next.events.length} appointments and ${next.notes.length} side notes?`)) return; data = next; await persist('Backup imported on this device.'); render(); } catch { showToast('This backup does not have the expected format. Choose a Booking Side Notes backup exported by this app.'); } });
  document.querySelector('#reset-demo')?.addEventListener('click', async () => { await discardDemoData(); data = sampleData(); await saveData(data, 'demo'); selectedDate = '2026-08-28'; pendingMessage = 'Demo reset to the original sample day.'; render(); }); document.querySelector('#start-real')?.addEventListener('click', async () => { await discardDemoData(); await navigate('/'); });
  document.querySelector('#apply-update')?.addEventListener('click', () => { updatingApp = true; void navigator.serviceWorker.getRegistration().then((registration) => registration?.waiting?.postMessage({ type: 'SKIP_WAITING' })); });
}
function updateNetworkState(): void { const el = document.querySelector('#network-state'); if (el) el.innerHTML = `<span aria-hidden="true">●</span> ${navigator.onLine ? 'On device' : 'Offline · on device'}`; }
async function registerServiceWorker(): Promise<void> { if (!('serviceWorker' in navigator)) return; try { const registration = await navigator.serviceWorker.register('/sw.js'); if (registration.waiting) document.querySelector<HTMLDivElement>('#update-toast')!.hidden = false; registration.addEventListener('updatefound', () => registration.installing?.addEventListener('statechange', () => { if (registration.installing?.state === 'installed' && navigator.serviceWorker.controller) document.querySelector<HTMLDivElement>('#update-toast')!.hidden = false; })); navigator.serviceWorker.addEventListener('controllerchange', () => { if (updatingApp) location.reload(); }); } catch { /* Browser policy can block service workers without blocking the local app. */ } }
async function start(_navigated = false): Promise<void> { const route = currentRoute(); setMetadata(route); if (route === 'not-found') { renderNotFound(); return; } storageMode = route === 'demo' ? 'demo' : 'real'; try { data = await loadData(storageMode); if (storageMode === 'demo' && !data.events.length && !data.notes.length) { data = sampleData(); await saveData(data, 'demo'); } selectedDate = storageMode === 'demo' ? '2026-08-28' : localDateKey(); render(); focusTitle(); } catch (error) { app.innerHTML = `<main id="main" class="fatal"><h1 id="page-title" tabindex="-1">Local storage is unavailable</h1><p>${esc(error instanceof Error ? error.message : 'Check this browser’s privacy settings and reload.')}</p></main>`; } }
addEventListener('online', updateNetworkState); addEventListener('offline', updateNetworkState); addEventListener('popstate', () => void start(true)); addEventListener('pageshow', (event: PageTransitionEvent) => { if (event.persisted) focusTitle(); }); void start(); void registerServiceWorker();
